package za.co.assignment.bankingSystem.service;

import org.springframework.stereotype.Service;

@Deprecated
@Service("ClientAccountService")
public class ClientAccountService {

    /*@Autowired
    private ClientAccountMapper clientAccountMapper;

    public ClientAccount getClientAccount(int id) {
        return clientAccountMapper.findByClientId(id);
    }

    public ClientAccount getClientAccount(String accNo) {
        return clientAccountMapper.findByAccountNo(accNo);
    }*/
}
